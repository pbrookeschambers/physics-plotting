import numpy as np
{{qoplots_import}}
import matplotlib.pyplot as plt
import matplotlib as mpl

# set the default styling options
{{styling}}

# Define the data
{{data_import}}

# Create the figure
fig, ax = plt.figure()

# Plot the data
plt.plot(data[:, 0], data[:, 1], {{plot_args}})

{{line_of_best_fit}}

# Figure styling
plt.xlabel(r'{{xlabel}}')
plt.ylabel(r'{{ylabel}}')
{{title}}
{{xlimits}}
{{ylimits}}
{{legend}}

# Save the figure
plt.savefig({{filename}}, {{save_args}})